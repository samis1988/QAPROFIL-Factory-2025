import { ComponentFixture, TestBed } from '@angular/core/testing';
import { PerimeteranglealumPage } from './perimeteranglealum.page';

describe('PerimeteranglealumPage', () => {
  let component: PerimeteranglealumPage;
  let fixture: ComponentFixture<PerimeteranglealumPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(PerimeteranglealumPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
